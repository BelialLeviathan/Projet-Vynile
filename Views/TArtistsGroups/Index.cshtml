@model IEnumerable<projet_boutique_vinyle.Models.TArtistsGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IsMember)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdArtistNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdGroupNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IsMember)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdArtistNavigation.IdArtist)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdGroupNavigation.IdGroup)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
