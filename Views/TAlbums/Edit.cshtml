@model projet_boutique_vinyle.Models.TAlbum

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TAlbum</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdAlbum" />
            <div class="form-group">
                <label asp-for="IdLabel" class="control-label"></label>
                <select asp-for="IdLabel" class="form-control" asp-items="ViewBag.IdLabel"></select>
                <span asp-validation-for="IdLabel" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
