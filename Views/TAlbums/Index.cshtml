@model IEnumerable<projet_boutique_vinyle.Models.TAlbum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdLabelNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdLabelNavigation.IdLabel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdAlbum">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdAlbum">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdAlbum">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
